# 前端风格选择器（Frontend Style Selector）— 产品需求文档

> **版本**：v1.0
> **日期**：2026-08-17
> **状态**：已确认

---

## 1. 产品概述

### 1.1 产品定位

纯前端 Web 应用。开发者在线浏览 74 个品牌设计风格，在 4 种典型业务页面（首页 / 产品轮播 / 登录 / 注册）上即时预览效果，可叠加 5 种动画，一键导出符合 awesome-design-md 规范的 DESIGN.md；可输入 DeepSeek API Key + 自定义想法进行 AI 润色重写。

### 1.2 产品目标

1. 帮助开发者快速找到适合自己项目的前端风格，生成适合自己项目的 DESIGN.md
2. 模块化预览前端风格和动画效果，支持 AI 高级润色

### 1.3 目标用户

| 用户类型 | 核心诉求 |
|---------|---------|
| 前端开发者（核心） | 挑选、比较风格，快速预览效果 |
| AI 应用开发者 | 为 agent 产出 DESIGN.md 规范文件 |
| 设计初学者 | 理解品牌设计语言差异 |

### 1.4 成功指标

- 风格切换 → 预览更新延迟 < 100ms
- 无 API 导出成功率 100%
- 有 API 且密钥有效时 AI 导出成功率 ≥ 95%
- 全部交互在当前页面完成，无需刷新

---

## 2. 背景与上游数据源

### 2.1 格式标准（Google Stitch DESIGN.md）

DESIGN.md 是由 Google Stitch 引入的设计系统文档规范，分两层：

- **YAML Frontmatter**：机器可读的设计令牌（`colors`、`typography`、`rounded`、`spacing`、`components` 等），存精确值
- **Markdown 正文**：人 / AI 可读的设计理念与使用指导

### 2.2 数据源仓库（VoltAgent/awesome-design-md）

GitHub 仓库 `VoltAgent/awesome-design-md`，包含从 74 个知名品牌网站提取的即用型 DESIGN.md 文件。每个品牌包含 3 个文件：

```
design-md/<brand>/
├── DESIGN.md          # 设计系统文档
├── preview.html       # 亮色预览
└── preview-dark.html  # 暗色预览
```

**74 个品牌分类**：

| 类别 | 数量 | 代表品牌 |
|------|------|---------|
| AI & LLM 平台 | 12 | Claude, Cohere, ElevenLabs, Minimax, Mistral AI, xAI |
| 开发者工具 & IDE | 7 | Cursor, Vercel, Raycast, Warp, Expo |
| 后端/数据库/DevOps | 8 | Supabase, MongoDB, Sentry, ClickHouse |
| 生产力 & SaaS | 7 | Linear, Notion, Cal.com, Resend, Zapier |
| 设计与创意工具 | 6 | Figma, Framer, Webflow, Miro |
| 金融科技 & 加密 | 7 | Stripe, Coinbase, Binance, Wise, Revolut |
| 电子商务 & 零售 | 5 | Airbnb, Shopify, Nike, Starbucks, Meta |
| 媒体 & 消费科技 | 12 | Apple, Spotify, NVIDIA, Uber, Pinterest |
| 汽车 | 7 | Tesla, BMW, Ferrari, Lamborghini, Bugatti |
| 复古网页 | 2 | Dell (1996), Nintendo.com (2001) |
| 其他 | 1 | Slack |

仓库实际章节命名与本产品导出标准章节的映射关系：

| 仓库 README 章节 | 导出标准章节 |
|---|---|
| Visual Theme & Atmosphere | Overview |
| Color Palette & Roles | Colors |
| Typography Rules | Typography |
| Layout Principles | Layout |
| Depth & Elevation | Elevation & Depth |
| Component Stylings | Shapes / Components |
| Do's and Don'ts | Do's and Don'ts |
| Responsive Behavior | Responsive Behavior |
| Agent Prompt Guide | 并入 Overview 或 AI 导出时补充 |

### 2.3 已确认的产品决策

| 决策项 | 选择 |
|--------|------|
| 风格数量 | 全部 74 种 |
| AI 润色范围 | 基于用户选定的基础风格 + 自定义需求，重写完整 DESIGN.md |
| 预览页面交互 | Tab 切换独立页面（首页 / 产品轮播 / 登录页 / 注册页） |
| 导出格式 | 严格遵循 awesome-design-md 格式（YAML frontmatter + 9 章节正文） |

---

## 3. 范围与约束

### 3.1 功能范围（In Scope）

- 74 种风格的列表浏览、搜索、选择、预览、导出
- 5 种动画效果的启用 / 禁用与预览
- 4 Tab 模拟浏览器窗口预览
- DeepSeek API 调用（仅浏览器内会话）、AI 重写 DESIGN.md、AI 汇总导出
- DESIGN.md 直接导出（离线可用）

### 3.2 非功能范围（Out of Scope）

- 用户注册 / 登录、账号体系、云端存储、多设备同步
- 自定义风格编辑（调色板手动改色）
- 导出为 Tailwind 主题 / CSS 变量文件等其他格式
- 后端服务、代理、限流、计费

### 3.3 技术约束

- 使用 Next.js 搭建前端页面（App Router + Client Components）
- 纯前端，无数据库，状态仅保存在当前页面内存
- DeepSeek API Key 不持久化（不写 localStorage / sessionStorage / cookies / IndexedDB），刷新即清空
- 内置 74 份 DESIGN.md 与预览资源随构建打包（static import 或 public/ 目录）
- 内置风格本质为各效果的排列组合

---

## 4. 用户流程与用例

### 4.1 核心用户流程（5 步）

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  1. 打开网站  │───▶│  2. 选择风格  │───▶│  3. 选择动画  │───▶│ 4. AI 润色   │───▶│  5. 导出     │
│             │    │             │    │             │    │  (可选)      │    │  DESIGN.md  │
│ 左侧两列表   │    │ 右侧即时     │    │ 右侧即时     │    │ 输入API Key  │    │             │
│ 右侧默认预览 │    │ 更新预览     │    │ 叠加动画     │    │ +自定义想法  │    │ 内置直出/    │
│             │    │             │    │             │    │             │    │ AI汇总导出  │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
```

1. 用户打开网站，左侧显示风格选择列表（74 种）和动画效果选择列表（5 种），右侧模拟浏览器窗口显示默认风格、无动画效果的首页 DEMO
2. 用户选择风格，右侧预览窗口 4 个 Tab 页面即时按新设计令牌重渲染
3. 用户选择动画效果，右侧预览窗口即时叠加动画效果
4. 如内置风格不满意，用户在界面上方填入 DeepSeek API Key，输入自定义前端想法，点击 AI 润色生成重写后的 DESIGN.md
5. 用户确认风格和动画后点击导出：内置内容直接导出；有 API 则由 DeepSeek 汇总导出

### 4.2 用例清单

| 编号 | 用例 | 优先级 |
|---|---|---|
| UC-01 | 浏览 / 搜索 74 种风格 | P0 |
| UC-02 | 选择风格并即时预览 | P0 |
| UC-03 | 选择动画效果并即时预览 | P0 |
| UC-04 | Tab 切换 4 个演示页面 | P0 |
| UC-05 | 直接导出所选风格 DESIGN.md | P0 |
| UC-06 | 输入 API Key + 想法 → AI 重写 DESIGN.md | P1 |
| UC-07 | AI 汇总导出（重写后文件下载） | P1 |
| UC-08 | 刷新 / 关闭页面后状态清零 | P0 |

---

## 5. 功能需求（FR）

### 5.1 FR-01 风格选择列表

| 维度 | 说明 |
|------|------|
| **输入** | 内置 74 条风格元数据（id, 名称, 分类, 原始 DESIGN.md 文本） |
| **输出** | 左侧面板渲染列表；选中项高亮；触发 `selectedStyleId` 更新 |
| **交互逻辑** | 分类分组展示（沿用仓库分类）；名称关键字搜索（前端过滤，纯内存）；点击选中 → 预览区按新令牌重渲染；仅允许单选；默认选中第一项 |
| **边界情况** | 搜索无结果显示空态文案；数据加载失败显示错误态并禁用导出 |

### 5.2 FR-02 动画效果选择列表

| 维度 | 说明 |
|------|------|
| **输入** | 5 条动画配置 |
| **输出** | 左侧动画列表；选中项高亮；触发 `selectedAnimationId` 更新 |
| **交互逻辑** | 单选 + 可取消（每个动画有明确页面作用域，多选会冲突）；提供"无动画"默认态；点击选中 → 预览区注入动画 CSS/JS；切换 Tab 时仅在匹配页面生效 |
| **边界情况** | 风格切换后动画保持选中（动画独立于风格）；动画与当前 Tab 不匹配时提示"该动画作用于 XX 页面" |

**5 种动画效果**：

| 序号 | 动画名称 | 说明 |
|------|---------|------|
| 1 | 滚动浮现 | 页面元素随滚动进入视口时淡入上移 |
| 2 | Hero 错峰入场 | Hero 区标题、副标题、CTA 按钮逐项延迟入场 |
| 3 | 自动轮播产品展示 | 产品幻灯片自动切换播放 |
| 4 | 能力跑马灯 | 能力标签横向无限循环滚动 |
| 5 | 数字/条形生长与悬停微交互 | 数字 count-up 动画 + 卡片 hover 缩放效果 |

### 5.3 FR-03 预览窗口（模拟浏览器）

| 维度 | 说明 |
|------|------|
| **输入** | `selectedStyleId` + `selectedAnimationId` + `activeTab` |
| **输出** | 右侧模拟浏览器窗口（地址栏 + 窗口控制点装饰），Tab 栏 4 个按钮，内容区渲染当前页 |
| **交互逻辑** | 地址栏显示虚拟 URL（随 Tab 变化）；Tab 点击切换页面；预览内容完全由设计令牌驱动（无硬编码品牌色）；提供"刷新预览"按钮重置动画状态 |
| **边界情况** | 风格令牌缺失字段用默认值兜底；图片使用占位（纯色/渐变块 + 文字） |

**4 个 Tab 页面**：

| Tab | 名称 | 用途 |
|-----|------|------|
| Home | 首页 | 展示导航、Hero、特性卡片、统计数字、跑马灯、页脚 |
| Carousel | 产品轮播 | 展示自动轮播、产品卡片网格 |
| Login | 登录页 | 展示表单组件及多种状态 |
| Register | 注册页 | 展示更丰富的表单组件及状态 |

### 5.4 FR-04 AI 润色面板

| 维度 | 说明 |
|------|------|
| **输入** | DeepSeek API Key（可选）、自定义想法文本（textarea，可空） |
| **输出** | AI 状态机（idle / loading / success / error）；成功时展示重写后的 DESIGN.md 预览，解锁"AI 导出" |
| **交互逻辑** | API Key 输入框 `type=password`，仅存内存；点击"AI 润色"→ 校验 Key → 组装 Prompt → 调用 API；请求中禁用按钮显示加载态；成功展示结果；失败展示错误信息与重试按钮 |
| **边界情况** | Key 无效（401）提示检查；超时（30s）提示重试；返回格式异常时展示原文供手动复制 |

### 5.5 FR-05 导出功能

| 维度 | 说明 |
|------|------|
| **输入** | 当前 `selectedStyleId`、可选 AI 重写结果 |
| **输出** | `.md` 文件下载（浏览器 Blob 下载） |
| **交互逻辑** | 无 AI：直接导出所选风格原始 DESIGN.md；有 AI 且润色成功：显示"导出原始 / 导出 AI 重写版"两个按钮；AI 导出前校验 frontmatter + 9 章节完整 |
| **文件命名** | `DESIGN-<styleId>.md` / `DESIGN-<styleId>-ai.md` |

### 5.6 FR-06 全局状态与数据加载

| 维度 | 说明 |
|------|------|
| **输入** | 内置数据源（74 份 DESIGN.md + 元数据 + 动画配置） |
| **输出** | 单例状态 store（zustand 或 React Context + useReducer） |
| **交互逻辑** | 应用初始化时异步解析 74 份 DESIGN.md 的 YAML frontmatter，构建风格→令牌映射表；状态全部存内存，不支持持久化；派生数据用 selector / useMemo 缓存 |
| **边界情况** | 部分 DESIGN.md 的 YAML 解析失败 → 标记为"降级样式"，用默认令牌渲染 + 控制台警告 |

---

## 6. 数据结构设计

### 6.1 页面状态（AppState）

```typescript
interface AppState {
  // 选择状态
  selectedStyleId: string | null;        // 当前风格 id，默认首个
  selectedAnimationId: string | null;    // 当前动画 id，默认 null（无动画）
  activeTab: 'home' | 'carousel' | 'login' | 'register';

  // AI 相关（仅内存，不持久化）
  apiKey: string;                        // DeepSeek API Key
  customIdea: string;                    // 自定义想法
  aiStatus: 'idle' | 'loading' | 'success' | 'error';
  aiError: string | null;
  aiResult: { rawText: string; doc: ParsedDesignDoc } | null;

  // 数据
  styles: StyleMeta[];                   // 74 条风格元数据
  animations: AnimationMeta[];           // 5 条动画配置
  searchQuery: string;                   // 风格搜索词
}
```

### 6.2 风格元数据（StyleMeta）

```typescript
interface StyleMeta {
  id: string;            // slug，如 'claude'、'tesla'，用作文件命名与 key
  name: string;          // 展示名，如 'Claude'
  category: string;      // 仓库分类名
  raw: string;           // 原始 DESIGN.md 文本（构建时静态导入）
  tokens: DesignTokens;  // 解析 YAML frontmatter 得到的令牌对象
  fallback?: boolean;    // 是否降级（YAML 解析失败）
}
```

### 6.3 DESIGN.md 文档结构

**YAML Frontmatter（机器可读令牌）**：

```yaml
---
version: alpha
name: <brand>-Inspired-design-analysis
description: 设计语言描述

colors:          # 颜色令牌（语义名 + 十六进制值）
  primary: "#171717"
  canvas: "#ffffff"
  ink: "#171717"
  # ...

typography:      # 排版层级（字体族/字号/字重/行高/字间距）
  display-xl:
    fontFamily: Geist, Inter, system-ui, sans-serif
    fontSize: 48px
    fontWeight: 600
    lineHeight: 48px
    letterSpacing: -2.4px
  # ...

rounded:         # 圆角令牌
  sm: 6px
  md: 8px
  pill: 9999px

spacing:         # 间距令牌
  xs: 8px
  md: 16px
  lg: 24px

components:      # 组件定义（使用 {colors.xxx} 引用语法）
  button-primary:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.on-primary}"
    typography: "{typography.button-lg}"
    rounded: "{rounded.pill}"
    padding: "0px {spacing.sm}"
---
```

**Markdown 正文（9 标准章节，导出强制模板）**：

| 序号 | 章节标题 | 内容 |
|------|---------|------|
| 1 | `## Overview` | 视觉主题、氛围、设计哲学、关键特征列表 |
| 2 | `## Colors` | 语义色名 + 色值 + 用途角色（Brand & Accent / Surface / Text / Semantic） |
| 3 | `## Typography` | 字体族、字号层级表、排版原则、开源替代字体建议 |
| 4 | `## Layout` | 栅格系统、间距体系、容器宽度、留白哲学 |
| 5 | `## Elevation & Depth` | 阴影层级表、装饰深度 |
| 6 | `## Shapes` | 圆角体系表、图片几何 |
| 7 | `## Components` | 按钮 / 卡片 / 输入框 / 导航 / 签名组件及状态 |
| 8 | `## Do's and Don'ts` | 设计护栏与反模式清单 |
| 9 | `## Responsive Behavior` | 断点、触控目标、折叠策略、图片行为 |

**解析器输出**：

```typescript
type ParsedDesignDoc = {
  frontmatter: Record<string, unknown>;  // YAML 令牌
  content: string;                        // Markdown 正文
  sections: Record<SectionKey, string>;   // 9 章节切分结果
};
// 使用 gray-matter 解析 frontmatter，正则切分 Markdown 章节
```

### 6.4 动画配置（AnimationMeta）

```typescript
interface AnimationMeta {
  id: string;                 // 'scroll-reveal' | 'hero-staggered' | 'auto-carousel' | 'capability-marquee' | 'number-bar-growth'
  name: string;               // 中文名
  description: string;        // 说明
  targetPages: TabKey[];      // 生效页面
  targetSelector: string;     // 作用于预览 DOM 的选择器
  cssClass: string;           // 注入的 CSS 类名前缀
  css?: string;               // 内联样式字符串
  jsBehavior?: 'intersection-observer' | 'stagger' | 'interval-carousel' | 'marquee' | 'count-up';
  config: Record<string, unknown>; // 参数：如 stagger.delay=80ms、carousel.interval=3s
}
```

### 6.5 主题令牌 → CSS 变量映射

解析 frontmatter 后生成 CSS 变量注入预览容器：

```css
/* 示例：令牌 → CSS 变量 */
--brand-primary: #1A1C1E;
--brand-secondary: #6C7278;
--font-h1: 'Public Sans', system-ui;
--font-h1-size: 3rem;
--radius-sm: 4px;
--radius-md: 8px;
--spacing-sm: 8px;
--spacing-md: 16px;
--component-button-bg: var(--brand-primary);
```

预览页组件统一引用这些变量，保证 74 风格一键换肤。

---

## 7. DeepSeek API 集成方案

### 7.1 集成方式

纯前端直连 DeepSeek OpenAI 兼容接口（符合"无后端 / 无数据库"约束）：

| 配置项 | 值 |
|--------|-----|
| Endpoint | `https://api.deepseek.com/chat/completions` |
| Model | `deepseek-chat`（默认；高级润色可切换 `deepseek-reasoner`） |
| 鉴权 | `Authorization: Bearer <用户输入的 Key>` |
| 代理 | 无需 Next.js API Route 代理（避免引入服务端） |

> 备注：Key 由用户本人输入、用于其本人会话，直连浏览器是合理方案。若未来遇到 CORS 限制，可降级为 Next.js Route Handler 透传（本期不实现）。

### 7.2 请求流程

1. 用户点击"AI 润色"
2. 前端组装消息体：
   - `system`：DESIGN.md 格式专家提示词（见 7.4）
   - `user`：`[基础风格 DESIGN.md 全文] + [用户自定义想法] + [输出约束]`
3. `fetch` POST，配置 `temperature: 0.7`、`max_tokens: 8000`、`stream: false`
4. 请求期间置 `aiStatus='loading'`，超时 30s（`AbortController`）

### 7.3 响应流程与解析

1. 校验 HTTP 200，解析 `choices[0].message.content` 得 Markdown 文本
2. 用 gray-matter 解析返回文本：frontmatter 必须可解析且含 `colors` 等核心字段，正文必须含 9 个章节标题
3. 校验通过 → 存 `aiResult`，状态置 `success`，面板展示文本
4. 校验失败 → 提示"AI 输出格式不完整"，仍展示原文供复制，不阻断手动导出

### 7.4 Prompt 设计

**System 提示词要点**：

- 角色：资深设计系统工程师 + AI 友好的设计文档写作者
- 输出格式强约束：YAML frontmatter 定义设计令牌（colors/typography/rounded/spacing/components），Markdown 正文必须包含且仅包含 9 个标准章节
- 风格基调：保留基础风格的视觉 DNA，融合用户自定义需求；禁止脱离令牌凭空造色值；Do's and Don'ts 面向 AI 生成场景写护栏

**User 提示词模板**：

```
请基于以下基础设计风格，结合我的需求，重写一份完整的 DESIGN.md。

【基础风格 DESIGN.md】
{基础风格的完整 DESIGN.md 文本}

【我的自定义需求】
{用户输入的自定义想法}

【要求】
1. 输出必须是完整可用的 Markdown 文档，以 --- frontmatter 开头。
2. 保留基础风格的核心理念，将我的需求融入颜色 / 排版 / 组件等章节。
3. 必须包含全部 9 个章节（Overview, Colors, Typography, Layout, Elevation & Depth,
   Shapes, Components, Do's and Don'ts, Responsive Behavior），不要省略、不要新增。
```

### 7.5 安全与隐私处理

| 安全措施 | 说明 |
|---------|------|
| Key 存储 | 只存 React 内存 state，禁止写入 localStorage / sessionStorage / cookies / IndexedDB |
| 生命周期 | 关闭 / 刷新页面即清空（内存态自然销毁） |
| 输入框 | `type=password`，禁止明文回显到 URL / 日志 |
| 网络请求 | 仅发往 `api.deepseek.com`，无任何第三方上报 |
| 服务端 | 不引入服务端，Key 不经过任何中转 |

### 7.6 错误处理与重试

| 错误类型 | HTTP 状态 | 表现 | 处理 |
|---------|----------|------|------|
| 无效 Key | 401 / 403 | 鉴权失败 | 提示检查 Key，重置 loading |
| 限流 | 429 | 请求过多 | 提示稍后重试，提供重试按钮 |
| 服务异常 | 5xx | 服务器错误 | 提示网络错误 + 重试 |
| 超时 | — | 30s 无响应 | AbortController 中止，提示重试 |
| 格式不合法 | — | 解析失败 | 展示原文 + 手动复制提示 |

---

## 8. 导出逻辑设计

### 8.1 直接导出（无 API / 未润色）

- **数据源**：`StyleMeta.raw`（所选风格的原始 DESIGN.md 文本）
- **流程**：点击"导出 DESIGN.md" → `new Blob([raw], { type: 'text/markdown' })` → `URL.createObjectURL` → 触发 `<a download>` 下载 → 释放 objectURL
- **产出**：`DESIGN-<styleId>.md`，内容与上游仓库一致，100% 保真
- **前置条件**：无（离线可用）

### 8.2 AI 汇总导出（有 API 且润色成功）

- **数据源**：`aiResult.rawText`（AI 重写后的完整文档）
- **流程**：导出前再次校验（frontmatter 可解析 + 9 章节齐全） → 通过则 Blob 下载 `DESIGN-<styleId>-ai.md` → 不通过则提示格式不完整，询问是否仍导出
- **前置条件**：已完成 FR-04 润色且状态为 `success`

### 8.3 两种模式差异对比

| 维度 | 直接导出 | AI 汇总导出 |
|------|---------|------------|
| 前置条件 | 选中风格即可 | API Key + 润色成功 |
| 数据来源 | 内置原始 DESIGN.md | DeepSeek 重写结果 |
| 内容 | 100% 原样 | 融合自定义需求 |
| 耗时 | 即时 | 数秒 ~ 30s |
| 文件名 | `DESIGN-<id>.md` | `DESIGN-<id>-ai.md` |
| 失败风险 | 无 | 格式校验失败可降级为原始导出 |

### 8.4 导出文件规范校验

校验项：

1. frontmatter 存在且可解析
2. 含 `name` 字段
3. 正文含全部 9 个 `##` 章节标题

校验失败处理：AI 导出时阻止下载并提示；直接导出时（原始文件天然合规）跳过校验。

---

## 9. 预览 DEMO 页面设计要点

### 9.1 设计原则

- **令牌驱动**：所有样式只引用 6.5 节的 CSS 变量，禁止硬编码任何品牌色 / 字体
- **品牌中性内容**：占位文案与图片不模仿任何真实品牌，统一使用虚构品牌（如 "Nova"），避免版权与误导
- **通用结构**：4 个页面覆盖典型 SaaS / 电商信息架构，保证 74 风格都有可对比的展示舞台
- **固定视口**：预览容器固定为桌面视口（如 1280×720），内嵌缩放以适配浏览器窗口

### 9.2 Tab 1 首页（Home）

| 区域 | 内容 | 承载动画 |
|------|------|---------|
| 顶部导航栏 | Logo + 4 个菜单项 + "登录 / 免费试用"按钮 | — |
| Hero 区 | 主标题（h1）、副标题、双 CTA 按钮（primary / secondary） | Hero 错峰入场 |
| 能力特性区 | 3 张卡片（图标 + 标题 + 描述） | 滚动浮现、数字/条形生长+悬停 |
| 数据统计区 | 4 个数字指标 | 数字生长 + 悬停微交互 |
| 能力跑马灯区 | 一行横向滚动的能力标签 | 能力跑马灯 |
| 页脚 | 链接组 + 版权 | 滚动浮现 |

### 9.3 Tab 2 产品轮播（Product Carousel）

| 区域 | 内容 | 承载动画 |
|------|------|---------|
| 自动轮播 | 3~5 张产品幻灯片（品牌名 + 价格 + 渐变占位图 + 简短描述） | 自动轮播 |
| 轮播控件 | 指示点 + 左右箭头 + 自动播放 | — |
| 产品卡片网格 | 轮播下方 3 张产品卡片，展示价格标签 / Badge 组件 | — |

### 9.4 Tab 3 登录页（Login）

| 区域 | 内容 |
|------|------|
| 卡片布局 | 居中单卡片：Logo、标题、副标题 |
| 表单 | 邮箱、密码输入框（展示 focus / hover / error 状态）、"记住我"复选框、"忘记密码"链接、登录按钮 |
| 错误演示 | 预置"必填校验"错误态样例（密码框红色描边 + 错误文案） |

### 9.5 Tab 4 注册页（Register）

| 区域 | 内容 |
|------|------|
| 卡片布局 | 居中单卡片：标题、副标题 |
| 表单 | 姓名、邮箱、密码、确认密码、同意条款复选框 |
| 主 CTA | 注册按钮；底部"已有账号？登录"链接 |
| 状态演示 | 表单组件与按钮的多种状态（disabled 状态演示） |

### 9.6 动画效果 → 页面元素映射

| 动画 id | 名称 | 作用页面 | 目标元素 | 触发方式 | 关键参数 |
|---------|------|---------|---------|---------|---------|
| scroll-reveal | 滚动浮现 | home | 特性卡片 / 统计区 / 页脚 | IntersectionObserver | 阈值 0.2、位移 24px、淡入 0.6s |
| hero-staggered | Hero 错峰入场 | home | Hero 区标题/副标题/CTA | 页面加载 / 切 Tab | 逐项 delay 80ms、上移淡入 |
| auto-carousel | 自动轮播 | carousel | 产品幻灯片 | 定时器 | interval 3s、缓动 ease-in-out |
| capability-marquee | 能力跑马灯 | home | 跑马灯标签行 | 无限循环 | 速度 40px/s、无缝循环 |
| number-bar-growth | 数字/条形生长+悬停 | home | 统计数字 + 特性卡片 | 可见性触发 + hover | count-up 1.5s、卡片 scale 1.03 + shadow |

> 交互约定：切换 Tab 时动画在该页重新触发；切换风格时动画配置不变，仅视觉令牌变化。

### 9.7 预览实现机制

- **隔离方案**：使用 `<iframe>` sandbox + `srcdoc` 注入完整 HTML + 令牌 CSS + 动画 CSS/JS，天然样式隔离，74 风格互不污染
- **风格切换**：每次切换重建 srcdoc
- **动画实现**：注入 `<style>` + 少量原生 JS，使用 IntersectionObserver / requestAnimationFrame，不引入第三方动画库

---

## 10. 非功能需求（NFR）

### 10.1 性能

- 74 份 DESIGN.md 在构建期打包为静态数据（首次解析异步、可缓存）
- 风格切换 → 预览重建 < 100ms（除首次）
- Bundle 保持轻量（预览使用 iframe，主包更小）

### 10.2 可用性 / 可访问性

- 列表支持键盘导航（上下键 + Enter）
- Tab 支持键盘切换
- 按钮、输入框带 `aria-label`
- 错误提示可被读屏识别

### 10.3 浏览器兼容性

- 目标为最近两个大版本的 Chrome / Safari / Edge / Firefox
- `AbortController`、`URL.createObjectURL` 等 API 需检查兼容性

### 10.4 安全

- 无后端、无持久化敏感信息
- iframe 使用 `sandbox` 限制脚本权限
- `Content-Security-Policy` 限制外联请求仅 `api.deepseek.com`（如部署静态站）

---

## 11. 里程碑与验收标准

### 11.1 开发里程碑

| 里程碑 | 内容 | 验收要点 |
|--------|------|---------|
| M1 骨架 | Next.js 项目、左右布局、数据导入与解析管线 | 74 风格列表可渲染，YAML 解析成功 |
| M2 预览核心 | 4 Tab 预览、令牌→CSS 变量、iframe srcdoc 渲染 | 风格切换即时生效 |
| M3 动画 | 5 种动画实现与映射 | 每个动画在目标页面正确触发 |
| M4 AI 集成 | API 面板、Prompt、解析校验 | 润色成功且格式合规 |
| M5 导出 | 双模式导出 + 校验 | 两种导出均产出合规 .md |

### 11.2 验收标准（Acceptance Criteria）

| 编号 | 验收标准 |
|------|---------|
| AC-1 | 74 风格全部可选且预览不出现品牌硬编码色 |
| AC-2 | 任意风格 × 任一动画 × 4 Tab 组合无运行时错误 |
| AC-3 | 刷新页面后 API Key 不存在于任何存储中 |
| AC-4 | 直接导出文件与所选风格原始 DESIGN.md 逐字节一致 |
| AC-5 | AI 导出的文件通过"frontmatter 可解析 + 9 章节齐全"自动校验 |
| AC-6 | 无 API Key 时全流程可用，仅 AI 相关功能禁用 |

---

## 12. 实现阶段关键文件路径规划（供开发参考）

```
前端风格选择器/
├── app/                          # Next.js App Router
│   ├── page.tsx                  # 主页面（左右布局 + 预览 + AI 面板）
│   └── layout.tsx                # 根布局
├── components/
│   ├── StyleList.tsx             # FR-01 风格列表
│   ├── AnimationList.tsx         # FR-02 动画列表
│   ├── PreviewWindow.tsx         # FR-03 模拟浏览器 + Tab
│   ├── demoPages/                # 4 个 DEMO 页模板
│   │   ├── Home.tsx
│   │   ├── Carousel.tsx
│   │   ├── Login.tsx
│   │   └── Register.tsx
│   ├── AIPanel.tsx               # FR-04 AI 润色
│   └── ExportButton.tsx          # FR-05 导出
├── lib/
│   ├── types.ts                  # 数据结构定义（第 6 节）
│   ├── parseDesign.ts            # DESIGN.md 解析器（gray-matter + 章节切分）
│   ├── tokensToCss.ts            # 令牌 → CSS 变量（6.5 节）
│   ├── animations/               # 5 种动画的 CSS/JS
│   └── deepseek.ts               # DeepSeek API 客户端 + 校验（第 7 节）
├── data/
│   ├── styles.json               # 74 风格元数据（构建脚本生成）
│   ├── design-md/                # 74 份原始 DESIGN.md（静态导入）
│   └── animations.ts             # 5 种动画配置
└── scripts/
    └── build-styles.mjs          # 构建期抓取/校验 74 份文件生成 styles.json
```
